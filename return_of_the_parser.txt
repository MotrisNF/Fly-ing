default values:
    nb_drones is mandatory. Can't be 0 or negative.
    gates are the entry and the exit of the grafo map. They need to be a posibbles position.
        I need to calculate if they are inside or outside the map, because is mandatory
        to be reacheables. color=none by default
    hubs are the zones of the map. They have a UNIC name, a x and y posible position
        the kind of the zone ()

dict{
    nb_drones: int,
    gates: tuple(tuble(x: int, y: int, color: str)), # entry, exit
    hubs: dict{name: str, values: list[x: int, y: int, zone: str, max_drones: int, color: str]},
    conections: list[tuple(pos1: str, pos2: str, capacity: int)]
    }